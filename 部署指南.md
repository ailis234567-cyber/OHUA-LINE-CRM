# 🌐 生成在线网址指南

有几种方式可以让你的网站从外网访问：

## 方案 1: 使用 ngrok（最简单，推荐）⭐

### 安装 ngrok
```bash
# Mac 使用 Homebrew
brew install ngrok

# 或者下载安装包
# 访问 https://ngrok.com/download
```

### 使用步骤
1. **注册 ngrok 账号**（免费）
   - 访问 https://dashboard.ngrok.com/signup
   - 注册后获取 authtoken

2. **配置 ngrok**
   ```bash
   ngrok config add-authtoken 你的authtoken
   ```

3. **启动你的 Web 应用**
   ```bash
   python web_app.py
   ```
   记住使用的端口（比如 5001）

4. **启动 ngrok**
   ```bash
   ngrok http 5001
   ```
   或者如果使用其他端口：
   ```bash
   ngrok http 8080
   ```

5. **获取在线网址**
   ngrok 会显示类似这样的信息：
   ```
   Forwarding  https://abc123.ngrok-free.app -> http://localhost:5001
   ```
   这个 `https://abc123.ngrok-free.app` 就是你的在线网址！

### ngrok 优点
- ✅ 免费版可用
- ✅ 自动 HTTPS
- ✅ 简单易用
- ✅ 实时显示访问日志

---

## 方案 2: 使用 localtunnel（无需注册）

### 安装
```bash
npm install -g localtunnel
```

### 使用
```bash
# 启动你的 Web 应用
python web_app.py

# 在另一个终端启动 localtunnel
lt --port 5001
```

会生成类似这样的网址：
```
your url is: https://random-name.loca.lt
```

### localtunnel 优点
- ✅ 完全免费
- ✅ 无需注册
- ✅ 自动 HTTPS

---

## 方案 3: 使用 Cloudflare Tunnel（免费，稳定）

### 安装
```bash
# Mac
brew install cloudflare/cloudflare/cloudflared
```

### 使用
```bash
# 启动你的 Web 应用
python web_app.py

# 启动 Cloudflare Tunnel
cloudflared tunnel --url http://localhost:5001
```

会生成一个随机网址，每次启动可能不同。

### 创建永久网址（推荐）
```bash
# 登录 Cloudflare
cloudflared tunnel login

# 创建隧道
cloudflared tunnel create my-tunnel

# 运行隧道
cloudflared tunnel run my-tunnel
```

---

## 方案 4: 部署到云服务器

### 选项 A: Vercel（免费，简单）
1. 安装 Vercel CLI
   ```bash
   npm install -g vercel
   ```

2. 部署
   ```bash
   vercel
   ```

### 选项 B: Railway（免费额度）
1. 访问 https://railway.app
2. 连接 GitHub 仓库
3. 自动部署

### 选项 C: Render（免费）
1. 访问 https://render.com
2. 创建 Web Service
3. 连接 GitHub 或直接部署

---

## 方案 5: 使用 frp（自建内网穿透）

如果你有自己的服务器，可以使用 frp：
1. 在服务器上运行 frps
2. 在本地运行 frpc
3. 配置端口映射

---

## 🚀 快速开始（推荐 ngrok）

### 1. 安装 ngrok
```bash
brew install ngrok
```

### 2. 注册并获取 token
访问：https://dashboard.ngrok.com/signup

### 3. 配置
```bash
ngrok config add-authtoken 你的token
```

### 4. 启动
```bash
# 终端1: 启动 Web 应用
python web_app.py

# 终端2: 启动 ngrok
ngrok http 5001
```

### 5. 访问
使用 ngrok 提供的网址，比如：
```
https://abc123.ngrok-free.app
```

---

## 🔒 安全建议

1. **添加访问密码**（如果使用内网穿透）
2. **使用 HTTPS**（ngrok 自动提供）
3. **限制访问 IP**（如果部署到服务器）
4. **定期更新依赖**

---

## 📝 注意事项

- 免费版 ngrok 网址每次启动可能不同
- 免费版 localtunnel 网址是随机的
- 如果需要固定网址，需要付费版或使用云服务器
- 确保防火墙允许相应端口

---

## 🎯 推荐方案

**快速测试**: 使用 **localtunnel**（无需注册）  
**稳定使用**: 使用 **ngrok**（免费版）  
**生产环境**: 部署到 **Vercel** 或 **Railway**

