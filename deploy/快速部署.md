# ⚡ 快速部署到 Linux VPS

## 一键部署（推荐）

### 步骤 1: 在本地打包

```bash
cd /Users/huihui/live-monitor
bash deploy/deploy.sh
```

会生成 `live-monitor-vps.tar.gz` 文件。

### 步骤 2: 上传到 VPS

```bash
scp live-monitor-vps.tar.gz root@你的VPS_IP:/opt/
```

### 步骤 3: 在 VPS 上安装

```bash
ssh root@你的VPS_IP

cd /opt
tar -xzf live-monitor-vps.tar.gz
cd live-monitor-vps
bash install.sh
```

### 步骤 4: 配置和启动

```bash
# 配置监控区域（需要先设置好）
cd /opt/live-monitor
source venv/bin/activate
nano config.yaml  # 编辑配置

# 启动服务
systemctl start live-monitor
systemctl start live-monitor-web
systemctl enable live-monitor
systemctl enable live-monitor-web
```

### 步骤 5: 访问

打开浏览器：`http://你的VPS_IP:5001`

---

## 📋 完整清单

- [ ] 打包文件（本地 Mac）
- [ ] 上传到 VPS
- [ ] 运行安装脚本
- [ ] 配置监控区域
- [ ] 启动服务
- [ ] 开放防火墙端口
- [ ] 测试访问

---

## ⚠️ Linux 特殊注意事项

1. **截图工具**：Linux 需要安装截图工具
   ```bash
   # Ubuntu/Debian
   apt-get install scrot
   
   # CentOS/RHEL
   yum install scrot
   ```

2. **图形环境**：需要 X11 或虚拟显示
   ```bash
   # 如果使用虚拟显示
   apt-get install xvfb
   Xvfb :99 -screen 0 1024x768x24 &
   export DISPLAY=:99
   ```

3. **Retina 模式**：Linux 不需要，设置 `retina: false`

4. **权限**：确保有截图权限

---

## 🔧 如果遇到问题

查看详细日志：
```bash
journalctl -u live-monitor -f
journalctl -u live-monitor-web -f
```

